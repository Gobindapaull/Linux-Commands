# `/etc/shadow`

```bash
cat /etc/shadow
```

If permission is denied:

```bash
sudo cat /etc/shadow
```

* `/etc/shadow` → Stores Linux user password hashes and password-aging information.
* `sudo` → Runs the command with root privileges.

⚠️ Contains sensitive information. Do not share it publicly.
